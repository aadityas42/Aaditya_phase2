# 15. Web Attack Forensics - Drone Alone

## Solution:

- This challenge is related to splunk.
- We use `index=windows_apache_access (cmd.exe OR powershell OR "powershell.exe" OR "Invoke-Expression") | table _time host clientip uri_path uri_query status` to detect suspicious web commands.
- Using this, we can find base-64 encoded strings which can be decoded using an online decoder.
- Next, we look for server-side errors using `index=windows_apache_error ("cmd.exe" OR "powershell" OR "Internal Server Error")`
- `index=windows_sysmon ParentImage="*httpd.exe"` traces suspicious process creation.
- `index=windows_sysmon *cmd.exe* *whoami*` helps us know what a process is doing.
- Finally, `index=windows_sysmon Image="*powershell.exe" (CommandLine="*enc*" OR CommandLine="*-EncodedCommand*" OR CommandLine="*Base64*")` discovers powershell commands even if they are base-64 encoded.

## Flag:
Nil.
 
## Concepts learnt:

- I learnt how to use some basic splunk queries.
