# 1. Linux CLI - Shell Bells

## Solution:

- This is pretty similar to what we learnt in linux linarium.
- First, we read the guide using `cat` and get one of the flags
- We use `grep` to filter the logs for failed logins.
- We use `find` to find a script with 'egg' in it and run the script to get another flag.
- We use `sudo su` and `cd /root` followed by history to check the bash history for the last flag.
  

## Flag:

```
THM{learning-linux-cli}
THM{sir-carrotbane-attacks}
THM{until-we-meet-again}
```

## Concepts learnt:

- Revision of Linux Linarium

## Notes:
- Nil.

## Resources:

- Nil.
